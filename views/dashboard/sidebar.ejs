<div class="flex flex-col w-16 sm:w-64 h-screen px-4 py-8 bg-grey-50 border-r">
  <!-- Sidebar Navigation -->
  <nav class="flex flex-col mt-4 space-y-1">
    <!-- Home Link -->
    <a
      href="/dashboard"
      class="<%= activePage === 'dashboard' ? 'bg-blue-200 ring-2 ring-blue-300 border-blue-400' : 'text-gray-700' %> flex items-center px-4 py-2 rounded-lg hover:bg-blue-100"
    >
      <!-- Icon Home -->
      <span class="w-6 h-6">🏠</span>
      <!-- Home Text -->
      <span class="hidden sm:inline ml-3">Home</span>
    </a>

    <!-- Create Link -->
    <a
      href="/dashboard/create"
      class="<%= activePage === 'create' ? 'bg-blue-200 ring-2 ring-blue-300 border-blue-400' : 'text-gray-700' %> flex items-center px-4 py-2 rounded-lg hover:bg-blue-100"
    >
      <!-- Icon Create -->
      <span class="w-6 h-6">✍️</span>
      <!-- Create Text -->
      <span class="hidden sm:inline ml-3">Create</span>
    </a>
  </nav>
</div>
